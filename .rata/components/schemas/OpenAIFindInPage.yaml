type: object
properties:
  pattern:
    type: string
    description: P
  type:
    type: string
    const: find_in_page
    enum:
      - find_in_page
    description: A
  url:
    type: string
    description: U
required:
  - pattern
  - url
  - type
description: |
  Action type "find_in_page": Searches for a pattern within a loaded page.
