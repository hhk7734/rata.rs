type: object
properties:
  call_id:
    type: string
    description: Call ID.
  output:
    $ref: "../schemas/FunctionCallOutputContent.yaml"
    description: Output.
  id:
    anyOf:
      - type: string
      - type: "null"
    description: Id.
  status:
    anyOf:
      - $ref: "../schemas/OutputStatus.yaml"
      - type: "null"
    description: Status.
required:
  - call_id
  - output
