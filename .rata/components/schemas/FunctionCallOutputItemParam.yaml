type: object
properties:
  call_id:
    type: string
  output:
    $ref: "../schemas/FunctionCallOutputContent.yaml"
  id:
    anyOf:
      - type: string
      - type: "null"
  status:
    anyOf:
      - $ref: "../schemas/OutputStatus.yaml"
      - type: "null"
required:
  - call_id
  - output
