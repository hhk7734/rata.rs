type: object
properties:
  type:
    type: string
    const: move
    enum:
      - move
    description: |
      Specifies the event type. For a move action, this property is always set to `move`.
  x:
    type: number
    description: X
  y:
    type: number
    description: Y
  keys:
    type: array
    items:
      type: string
    description: K
required:
  - y
  - x
  - type
description: M
