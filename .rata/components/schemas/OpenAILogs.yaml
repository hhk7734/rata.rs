type: object
properties:
  logs:
    type: string
    description: The logs output from the code interpreter.
  type:
    type: string
    const: logs
    enum:
    - logs
    description: The type of the output. Always `logs`.
required:
- logs
- type
description: The logs output from the code interpreter.
