type: object
properties:
  display_height:
    type: number
    description: H
  display_width:
    type: number
    description: W
  environment:
    anyOf:
      - type: string
        const: windows
        description: Must be `windows`.
      - type: string
        const: mac
        description: Must be `mac`.
      - type: string
        const: linux
        description: Must be `linux`.
      - $ref: ../schemas/OpenAI2.yaml
    description: T
  type:
    type: string
    const: computer_use_preview
    enum:
      - computer_use_preview
    description: T
required:
  - environment
  - display_width
  - display_height
  - type
description: T
