type: object
properties:
  custom:
    type: object
    description: Properties of the custom tool.
  type:
    type: string
    const: custom
    enum:
      - custom
    description: T
required:
  - custom
  - type
description: C
