type: object
properties:
  content:
    type: array
    items:
      $ref: ../schemas/AnthropicBashCodeExecutionOutputBlock.yaml
    description: Content.
  return_code:
    type: number
    description: Return code.
  stderr:
    type: string
    description: Stderr.
  stdout:
    type: string
    description: Stdout.
  type:
    type: string
    const: bash_code_execution_result
    enum:
      - bash_code_execution_result
    description: Must be `bash_code_execution_result`.
required:
  - content
  - return_code
  - stderr
  - stdout
  - type
