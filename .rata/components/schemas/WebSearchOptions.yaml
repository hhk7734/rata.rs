description: "`web_search_options` configuration."
type: object
properties:
  search_context_size:
    anyOf:
      - $ref: "../schemas/WebSearchContextSize.yaml"
      - type: "null"
    description: Search context size.
  user_location:
    anyOf:
      - $ref: "../schemas/WebSearchUserLocation.yaml"
      - type: "null"
    description: User location.
additionalProperties: false
