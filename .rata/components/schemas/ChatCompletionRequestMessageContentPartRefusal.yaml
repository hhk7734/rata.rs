description: |
  Body for `{"type":"refusal","refusal":"…"}` content parts. Only valid
  on assistant turns echoed back as input — refusal text from a prior
  response.
type: object
properties:
  refusal:
    type: string
    description: Refusal.
required:
  - refusal
