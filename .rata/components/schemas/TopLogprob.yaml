description: 'Values: the top-k alternative tokens at a generation step.'
type: object
properties:
  token:
    type: string
    description: Token.
  logprob:
    type: number
    format: float
    description: Logprob.
  bytes:
    anyOf:
      - type: array
        items:
          type: integer
          format: uint8
          minimum: 0
          maximum: 255
      - type: "null"
    description: Bytes.
required:
  - token
  - logprob
additionalProperties: false
