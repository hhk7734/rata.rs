description: "`tool_choice` request parameter."
anyOf:
  - allOf:
      - $ref: "../schemas/ToolChoiceAllowed.yaml"
      - type: object
        properties:
          type:
            type: string
            const: allowed_tools
        required:
          - type
  - allOf:
      - $ref: "../schemas/ToolChoiceFunction.yaml"
      - type: object
        properties:
          type:
            type: string
            const: function
        required:
          - type
  - allOf:
      - $ref: "../schemas/ToolChoiceMCP.yaml"
      - type: object
        properties:
          type:
            type: string
            const: mcp
        required:
          - type
  - allOf:
      - $ref: "../schemas/ToolChoiceCustom.yaml"
      - type: object
        properties:
          type:
            type: string
            const: custom
        required:
          - type
  - type: object
    properties:
      type:
        type: string
        const: apply_patch
    required:
      - type
  - type: object
    properties:
      type:
        type: string
        const: shell
    required:
      - type
  - $ref: "../schemas/HostedToolType.yaml"
  - $ref: "../schemas/ToolChoiceMode.yaml"
