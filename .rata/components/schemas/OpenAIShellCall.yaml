type: object
properties:
  action:
    type: object
    description: S
  call_id:
    type: string
    description: U
  type:
    type: string
    const: shell_call
    enum:
      - shell_call
    description: T
  id:
    type: string
    description: U
  environment:
    anyOf:
      - $ref: ../schemas/ResponseLocalEnvironment.yaml
      - $ref: ../schemas/ResponseContainerReference.yaml
    description: |
      Represents the use of a local environment to perform shell actions.
  status:
    anyOf:
      - type: string
        const: in_progress
        description: Must be `in_progress`.
      - type: string
        const: completed
        description: Must be `completed`.
      - type: string
        const: incomplete
        description: Must be `incomplete`.
    description: S
  created_by:
    type: string
    description: I
required:
  - environment
  - call_id
  - action
  - status
  - id
  - type
description: T
