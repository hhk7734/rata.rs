type: object
properties:
  call_id:
    type: string
    description: U
  operation:
    anyOf:
      - type: object
      - type: object
      - type: object
    description: |
      Values: the create_file, delete_file, or update_file operations applied via apply_patch.
  status:
    anyOf:
      - type: string
        const: in_progress
        description: Must be `in_progress`.
      - type: string
        const: completed
        description: Must be `completed`.
    description: S
  type:
    type: string
    const: apply_patch_call
    enum:
      - apply_patch_call
    description: T
  id:
    type: string
    description: U
  created_by:
    type: string
    description: I
required:
  - operation
  - call_id
  - status
  - id
  - type
description: T
