description: 'Message item inside `Item::Message`. Untagged; discriminated by `role`:

  `Output` requires `"assistant"`, `Input` accepts `"user"`/`"system"`/

  `"developer"`.'
anyOf:
  - $ref: "../schemas/InputOutputMessage.yaml"
  - $ref: "../schemas/InputMessage.yaml"
