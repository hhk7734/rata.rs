description: "`audio_url` payload (vLLM extension). Distinct from OpenAI's base64\n
  `input_audio` shape."
type: object
properties:
  url:
    type: string
    format: uri
  uuid:
    anyOf:
    - type: string
      format: uuid
    - type: "null"
required:
- url
additionalProperties: false
