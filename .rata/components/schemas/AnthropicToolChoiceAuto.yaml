type: object
properties:
  type:
    type: string
    const: auto
    enum:
      - auto
    description: Must be `auto`.
  disable_parallel_tool_use:
    type: boolean
    description: |
      Whether to disable parallel tool use.
      Default: `false`. If set to `true`, the model will output at most one tool use.
required:
  - type
description: M
