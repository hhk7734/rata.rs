type: object
properties:
  call_id:
    type: string
    description: I
  output:
    $ref: ../schemas/ResponseComputerToolCallOutputScreenshot.yaml
    description: C
  type:
    type: string
    const: computer_call_output
    enum:
      - computer_call_output
    description: T
  id:
    type: string
    description: U
  acknowledged_safety_checks:
    type: array
    items:
      type: object
    description: 'S'
  status:
    anyOf:
      - type: string
        const: completed
        description: Must be `completed`.
      - type: string
        const: incomplete
        description: Must be `incomplete`.
      - type: string
        const: failed
        description: Must be `failed`.
      - type: string
        const: in_progress
        description: Must be `in_progress`.
    description: 'S'
  created_by:
    type: string
    description: I
required:
  - call_id
  - output
  - status
  - id
  - type
description: O
