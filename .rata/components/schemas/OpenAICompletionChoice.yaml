type: object
properties:
  finish_reason:
    anyOf:
      - type: string
        const: stop
        description: Must be `stop`.
      - type: string
        const: length
        description: Must be `length`.
      - type: string
        const: content_filter
        description: Must be `content_filter`.
    description: 'R'
  index:
    type: number
    description: Index.
  logprobs:
    type: object
    description: Logprobs.
  text:
    type: string
    description: Text.
required:
  - finish_reason
  - index
  - logprobs
  - text
