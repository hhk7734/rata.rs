type: object
properties:
  content:
    anyOf:
      - type: string
      - type: array
        items:
          $ref: ../schemas/ChatCompletionContentPart.yaml
    description: C
  role:
    type: string
    const: user
    enum:
      - user
    description: R
  name:
    type: string
    description: O
required:
  - content
  - role
description: |
  Messages sent by an end user, containing prompts or additional context
  information.
