type: object
properties:
  type:
    type: string
    const: input_image
    enum:
      - input_image
    description: T
  detail:
    anyOf:
      - type: string
        const: low
        description: Must be `low`.
      - type: string
        const: high
        description: Must be `high`.
      - type: string
        const: auto
        description: Must be `auto`.
      - type: string
        const: original
        description: Must be `original`.
    description: D
  file_id:
    type: string
    description: I
  image_url:
    type: string
    description: U
required:
  - type
description: I
