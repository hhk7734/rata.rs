description: |
  Input to a `POST /v1/responses` request — a flat string or an array
   of [`InputItem`]s (messages, function calls, reasoning, hosted-tool
   items, ...). Mirrors Dynamo's `InputParam`.
anyOf:
  - type: string
  - type: array
    items:
      $ref: "../schemas/InputItem.yaml"
