type: object
properties:
  type:
    type: string
    const: any
    enum:
      - any
    description: Must be `any`.
  disable_parallel_tool_use:
    type: boolean
    description: |
      Whether to disable parallel tool use.
      Default: `false`. If set to `true`, the model will output exactly one tool use.
required:
  - type
description: M
