type: object
properties:
  role:
    anyOf:
      - type: string
        const: unknown
        description: Must be `unknown`.
      - type: string
        const: user
        description: Must be `user`.
      - type: string
        const: assistant
        description: Must be `assistant`.
      - $ref: ../schemas/OpenAI5.yaml
    description: R
  tools:
    type: array
    items:
      anyOf:
        - type: object
        - type: object
        - type: object
        - $ref: ../schemas/OpenAI12.yaml
    description: A
  type:
    type: string
    const: additional_tools
    enum:
      - additional_tools
    description: T
  id:
    type: string
    description: U
required:
  - tools
  - id
  - role
  - type
