type: object
properties:
  arguments:
    type: string
    description: J
  call_id:
    type: string
    description: U
  name:
    type: string
    description: N
  type:
    type: string
    const: function_call
    enum:
      - function_call
    description: T
  id:
    type: string
    description: U
  namespace:
    type: string
    description: N
  status:
    anyOf:
      - type: string
        const: in_progress
        description: Must be `in_progress`.
      - type: string
        const: completed
        description: Must be `completed`.
      - type: string
        const: incomplete
        description: Must be `incomplete`.
    description: 'S'
required:
  - name
  - call_id
  - arguments
  - type
description: 'T'
