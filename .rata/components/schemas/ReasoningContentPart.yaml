anyOf:
  - type: object
    properties:
      text:
        type: string
        description: Text.
      type:
        type: string
        const: reasoning_text
        description: Must be `reasoning_text`.
    required:
      - type
      - text
  - description: Forward-compatible catch-all.
