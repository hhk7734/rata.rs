anyOf:
  - type: object
    properties:
      text:
        type: string
      type:
        type: string
        const: reasoning_text
    required:
      - type
      - text
  - description: Forward-compatible catch-all.
