type: object
properties:
  type:
    type: string
    const: file_search
    enum:
      - file_search
    description: T
  vector_store_ids:
    type: array
    items:
      type: string
    description: I
  filters:
    anyOf:
      - $ref: ../schemas/OpenAIComparisonFilter.yaml
      - $ref: ../schemas/OpenAICompoundFilter.yaml
    description: F
  max_num_results:
    type: number
    description: M
  ranking_options:
    type: object
    description: Ranking options for search.
required:
  - vector_store_ids
  - type
description: T
