type: object
properties:
  text:
    type: string
    description: T
  type:
    type: string
    const: type
    enum:
      - type
    description: |
      Specifies the event type. For a type action, this property is always set to `type`.
required:
  - text
  - type
description: A
