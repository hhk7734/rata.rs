type: object
properties:
  content:
    type: array
    items:
      $ref: ../schemas/AnthropicCodeExecutionOutputBlock.yaml
    description: Content.
  return_code:
    type: number
    description: Return code.
  stderr:
    type: string
    description: Stderr.
  stdout:
    type: string
    description: Stdout.
  type:
    type: string
    const: code_execution_result
    enum:
      - code_execution_result
    description: Must be `code_execution_result`.
required:
  - content
  - return_code
  - stderr
  - stdout
  - type
