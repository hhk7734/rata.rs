type: object
properties:
  content:
    type: array
    items:
      $ref: ../schemas/AnthropicCodeExecutionOutputBlock.yaml
  return_code:
    type: number
  stderr:
    type: string
  stdout:
    type: string
  type:
    type: string
    const: code_execution_result
    enum:
      - code_execution_result
required:
  - content
  - return_code
  - stderr
  - stdout
  - type
