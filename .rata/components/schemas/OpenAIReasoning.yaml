type: object
properties:
  id:
    type: string
    description: U
  summary:
    type: array
    items:
      $ref: ../schemas/OpenAISummaryTextContent.yaml
    description: Reasoning summary content.
  type:
    type: string
    const: reasoning
    enum:
      - reasoning
    description: T
  content:
    type: array
    items:
      type: object
    description: Reasoning text content.
  encrypted_content:
    type: string
    description: 'E'
  status:
    anyOf:
      - type: string
        const: in_progress
        description: Must be `in_progress`.
      - type: string
        const: completed
        description: Must be `completed`.
      - type: string
        const: incomplete
        description: Must be `incomplete`.
    description: 'S'
required:
  - id
  - type
  - summary
description: 'D'
