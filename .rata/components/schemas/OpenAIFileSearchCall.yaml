type: object
properties:
  id:
    type: string
    description: U
  queries:
    type: array
    items:
      type: string
    description: Q
  status:
    anyOf:
      - type: string
        const: in_progress
        description: Must be `in_progress`.
      - type: string
        const: searching
        description: Must be `searching`.
      - type: string
        const: completed
        description: Must be `completed`.
      - $ref: ../schemas/OpenAI2.yaml
    description: 'S'
  type:
    type: string
    const: file_search_call
    enum:
      - file_search_call
    description: T
  results:
    type: array
    items:
      type: object
    description: R
required:
  - queries
  - status
  - id
  - type
description: 'R'
