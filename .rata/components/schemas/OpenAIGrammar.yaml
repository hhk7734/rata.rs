type: object
properties:
  definition:
    type: string
    description: G
  syntax:
    anyOf:
      - type: string
        const: lark
        description: Must be `lark`.
      - type: string
        const: regex
        description: Must be `regex`.
    description: S
  type:
    type: string
    const: grammar
    enum:
      - grammar
    description: Grammar format. Must be `grammar`.
required:
  - definition
  - syntax
  - type
description: G
