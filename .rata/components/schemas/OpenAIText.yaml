type: object
properties:
  type:
    type: string
    const: text
    enum:
      - text
    description: Unconstrained text format. Must be `text`.
required:
  - type
description: Unconstrained free-form text.
