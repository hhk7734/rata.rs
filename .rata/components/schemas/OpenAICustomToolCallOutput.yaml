type: object
properties:
  call_id:
    type: string
    description: C
  output:
    anyOf:
      - type: string
      - type: array
        items:
          $ref: ../schemas/ResponseInputText.yaml
      - $ref: ../schemas/ResponseInputImage.yaml
      - $ref: ../schemas/ResponseInputFile.yaml
    description: 'O'
  type:
    type: string
    const: custom_tool_call_output
    enum:
      - custom_tool_call_output
    description: T
  id:
    type: string
    description: U
  status:
    anyOf:
      - type: string
        const: in_progress
        description: Must be `in_progress`.
      - type: string
        const: completed
        description: Must be `completed`.
      - type: string
        const: incomplete
        description: Must be `incomplete`.
    description: 'S'
  created_by:
    type: string
    description: I
required:
  - call_id
  - output
  - status
  - id
  - type
description: O
