type: object
properties:
  type:
    anyOf:
      - type: string
        const: file_search
        description: Must be `file_search`.
      - type: string
        const: web_search_preview
        description: Must be `web_search_preview`.
      - type: string
        const: computer
        description: Must be `computer`.
      - $ref: ../schemas/OpenAI5.yaml
    description: 'T'
required:
  - type
description: |
  Indicates that the model should use a built-in tool to generate a response.
  [
