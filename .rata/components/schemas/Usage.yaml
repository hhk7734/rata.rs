type: object
properties:
  prompt_tokens:
    type: integer
    format: uint32
    minimum: 0
    description: Prompt tokens.
  total_tokens:
    type: integer
    format: uint32
    minimum: 0
    description: Total tokens.
  completion_tokens:
    type: integer
    format: uint32
    minimum: 0
    description: Completion tokens.
  prompt_tokens_details:
    description: |
      Always serialized (as `null` when absent) to mirror vLLM's wire
       shape, which clients pin against.
    anyOf:
      - $ref: "../schemas/PromptTokensDetails.yaml"
      - type: "null"
    default:
  completion_tokens_details:
    anyOf:
      - $ref: "../schemas/CompletionTokensDetails.yaml"
      - type: "null"
    description: Completion tokens details.
required:
  - prompt_tokens
  - total_tokens
  - completion_tokens
additionalProperties: false
