type: object
properties:
  container:
    anyOf:
      - type: string
      - type: object
    description: 'C'
  type:
    type: string
    const: code_interpreter
    enum:
      - code_interpreter
    description: T
required:
  - container
  - type
description: T
