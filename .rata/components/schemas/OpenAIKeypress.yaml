type: object
properties:
  keys:
    type: array
    items:
      type: string
    description: C
  type:
    type: string
    const: keypress
    enum:
      - keypress
    description: |
      Specifies the event type. For a keypress action, this property is always set to `keypress`.
required:
  - type
  - keys
description: C
