type: object
properties:
  type:
    type: string
    const: apply_patch
    enum:
      - apply_patch
    description: T
required:
  - type
description: |
  Allows the assistant to create, delete, or update files using unified diffs.
