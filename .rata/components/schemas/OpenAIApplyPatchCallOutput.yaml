type: object
properties:
  call_id:
    type: string
    description: U
  status:
    anyOf:
      - type: string
        const: completed
        description: Must be `completed`.
      - type: string
        const: failed
        description: Must be `failed`.
    description: S
  type:
    type: string
    const: apply_patch_call_output
    enum:
      - apply_patch_call_output
    description: T
  id:
    type: string
    description: U
  output:
    type: string
    description: Textual output returned by the apply patch tool.
  created_by:
    type: string
    description: I
required:
  - status
  - call_id
  - id
  - type
description: S
