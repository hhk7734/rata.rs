type: object
properties:
  type:
    type: string
    const: timeout
    enum:
      - timeout
    description: O
required:
  - type
description: |
  Indicates that the shell call exceeded its configured time limit.
