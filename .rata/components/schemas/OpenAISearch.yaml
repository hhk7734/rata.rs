type: object
properties:
  type:
    type: string
    const: search
    enum:
      - search
    description: A
  queries:
    type: array
    items:
      type: string
    description: S
  query:
    type: string
    description: S
  sources:
    type: array
    items:
      type: object
    description: S
required:
  - type
description: Action type "search" - Performs a web search query.
