type: object
properties:
  type:
    type: string
    const: wait
    enum:
    - wait
    description: Specifies the event type. For a wait action, this property is always
      set to `wait`.
required:
- type
description: A wait action.
