description: |
  Streaming option — request that lifecycle event snapshots embed
   `obfuscation` tokens.
type: object
properties:
  include_obfuscation:
    anyOf:
      - type: boolean
      - type: "null"
    description: Include obfuscation.
additionalProperties: false
