type: object
properties:
  allowed_domains:
    type: array
    items:
      type: string
    description: L
  type:
    type: string
    const: allowlist
    enum:
      - allowlist
    description: |
      Allow outbound network access only to specified domains. Must be `allowlist`.
  domain_secrets:
    type: array
    items:
      $ref: ../schemas/OpenAIContainerNetworkPolicyDomainSecret.yaml
    description: Domain-scoped secrets for allowlisted domains.
required:
  - allowed_domains
  - type
