type: object
properties:
  key:
    type: string
    description: K
  type:
    anyOf:
      - type: string
        const: eq
        description: Must be `eq`.
      - type: string
        const: ne
        description: Must be `ne`.
      - type: string
        const: gt
        description: Must be `gt`.
      - $ref: ../schemas/OpenAI5.yaml
    description: |
      Specifies the comparison operator: `eq`, `ne`, `gt`, `gte`, `lt`, `lte`, `in`, `nin`.
  value:
    anyOf:
      - type: string
      - type: number
      - type: boolean
      - type: array
        items:
          type: string
      - type: number
    description: V
required:
  - value
  - key
  - type
description: F
