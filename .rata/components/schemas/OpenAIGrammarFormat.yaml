type: object
properties:
  grammar:
    type: object
    description: Your chosen grammar.
  type:
    type: string
    const: grammar
    enum:
      - grammar
    description: Grammar format. Must be `grammar`.
required:
  - grammar
  - type
description: G
