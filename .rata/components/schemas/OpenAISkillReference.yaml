type: object
properties:
  skill_id:
    type: string
    description: I
  type:
    type: string
    const: skill_reference
    enum:
      - skill_reference
    description: References a skill created with the /v1/skills endpoint.
  version:
    type: string
    description: |
      Skill version. Use a positive integer or 'latest'. Omit for default.
required:
  - skill_id
  - type
