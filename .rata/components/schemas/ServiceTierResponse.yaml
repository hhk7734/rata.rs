description: "Service tier the backend reports it used. vLLM has no concept of tiers\n and always emits `None`; the field exists for spec completeness."
type: string
enum:
  - scale
  - default
  - flex
  - priority
