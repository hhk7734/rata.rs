type: object
properties:
  embedding:
    type: array
    items:
      type: number
    description: The embedding vector, which is a list of floats.
  index:
    type: integer
    description: The index of the embedding in the list of embeddings.
  object:
    type: string
    const: embedding
    description: The object type, which is always "embedding".
required:
  - embedding
  - index
  - object
description: An embedding generated by the model.
