type: object
properties:
  content:
    $ref: ../schemas/AnthropicCodeExecutionToolResultBlockContent.yaml
    description: |
      Code execution result with encrypted stdout for PFC + web_search results.
  tool_use_id:
    type: string
    description: Tool use ID.
  type:
    type: string
    const: code_execution_tool_result
    enum:
      - code_execution_tool_result
    description: Must be `code_execution_tool_result`.
required:
  - content
  - tool_use_id
  - type
