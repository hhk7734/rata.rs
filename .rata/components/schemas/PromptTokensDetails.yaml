type: object
properties:
  cached_tokens:
    anyOf:
    - type: integer
      format: uint32
      minimum: 0
    - type: "null"
  audio_tokens:
    anyOf:
    - type: integer
      format: uint32
      minimum: 0
    - type: "null"
additionalProperties: false
