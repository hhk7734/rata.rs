description: Stored prompt template reference.
type: object
properties:
  id:
    type: string
    description: Id.
  version:
    anyOf:
      - type: string
      - type: "null"
    description: Version.
  variables: {}
required:
  - id
additionalProperties: false
