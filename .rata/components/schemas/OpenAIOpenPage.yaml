type: object
properties:
  type:
    type: string
    const: open_page
    enum:
      - open_page
    description: A
  url:
    type: string
    description: U
required:
  - type
description: |
  Action type "open_page" - Opens a specific URL from search results.
