type: object
properties:
  id:
    type: string
    description: U
  result:
    type: string
    description: G
  status:
    anyOf:
      - type: string
        const: in_progress
        description: Must be `in_progress`.
      - type: string
        const: completed
        description: Must be `completed`.
      - type: string
        const: generating
        description: Must be `generating`.
      - type: string
        const: failed
        description: Must be `failed`.
    description: S
  type:
    type: string
    const: image_generation_call
    enum:
      - image_generation_call
    description: T
required:
  - status
  - id
  - result
  - type
description: I
