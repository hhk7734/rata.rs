type: object
properties:
  url:
    type: string
    format: uri
  title:
    anyOf:
    - type: string
    - type: "null"
  start_index:
    anyOf:
    - type: integer
      format: uint32
      minimum: 0
    - type: "null"
  end_index:
    anyOf:
    - type: integer
      format: uint32
      minimum: 0
    - type: "null"
required:
- url
additionalProperties: false
