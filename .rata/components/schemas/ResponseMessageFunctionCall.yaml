description: "Deprecated `function_call` echo. OpenAI keeps it on the response for\n
  backwards compatibility with pre-tools-API clients."
type: object
properties:
  name:
    type: string
  arguments:
    type: string
required:
- name
- arguments
additionalProperties: false
