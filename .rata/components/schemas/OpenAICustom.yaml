type: object
properties:
  name:
    type: string
    description: N
  type:
    type: string
    const: custom
    enum:
      - custom
    description: T
  defer_loading:
    type: boolean
    description: |
      Whether this tool should be deferred and discovered via tool search.
  description:
    type: string
    description: |
      Description of the custom tool, used to provide more context.
  format:
    $ref: ../schemas/OpenAICustomToolInputFormat.yaml
    description: I
required:
  - name
  - type
description: C
