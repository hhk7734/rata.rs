description: |
  Function declaration body inside a [`ChatCompletionTool`] entry.
type: object
properties:
  name:
    type: string
    description: Name.
  description:
    anyOf:
      - type: string
      - type: "null"
    description: Description.
  parameters: {}
  strict:
    anyOf:
      - type: boolean
      - type: "null"
    description: Strict.
required:
  - name
additionalProperties: false
