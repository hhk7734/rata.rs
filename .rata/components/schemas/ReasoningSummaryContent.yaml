anyOf:
  - type: object
    properties:
      text:
        type: string
        description: Text.
      type:
        type: string
        const: summary_text
        description: Must be `summary_text`.
    required:
      - type
      - text
  - description: Forward-compatible catch-all.
