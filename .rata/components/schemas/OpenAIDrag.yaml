type: object
properties:
  path:
    type: array
    items:
      type: object
    description: 'A'
  type:
    type: string
    const: drag
    enum:
      - drag
    description: |
      Specifies the event type. For a drag action, this property is always set to `drag`.
  keys:
    type: array
    items:
      type: string
    description: K
required:
  - path
  - type
description: D
