description: One entry in the request's `tools` array.
type: object
properties:
  type:
    $ref: "../schemas/ChatCompletionToolType.yaml"
    default: function
    description: Type.
  function:
    $ref: "../schemas/FunctionObject.yaml"
    description: Function.
required:
  - function
additionalProperties: false
