type: object
properties:
  type:
    type: string
    const: compaction_trigger
    enum:
      - compaction_trigger
    description: T
required:
  - type
description: Compacts the current context. Must be the final input item.
