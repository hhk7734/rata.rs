description: User / system / developer input message.
type: object
properties:
  content:
    type: array
    items:
      $ref: "../schemas/InputContent.yaml"
  role:
    $ref: "../schemas/InputRole.yaml"
  status:
    anyOf:
      - $ref: "../schemas/OutputStatus.yaml"
      - type: "null"
required:
  - content
  - role
additionalProperties: false
